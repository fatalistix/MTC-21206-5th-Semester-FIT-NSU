/* 2) Написать функцию, считающую сумму целых чисел в массиве.
 *
 * Использовать функции: fold.
 */


import runtime;
import ds/array;

main() {
    arr : [int] = [1, 22, 333, 4444, 55555];
    println(fold(arr, 0, \x, y -> x + y));
    quit(0);
}